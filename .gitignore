# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Real credentials. `.env.example` is the checked-in template.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Local tooling and editor state, not project source.
.claude/

# Server bundle for the Vercel function. Build output, like dist/.
api-build

# Local tooling and editor state, not project source.
.playwright-mcp/

# Markdown is not published to the remote. Project documentation lives in
# README.rst; design notes, plans and specs stay local to the working copy.
*.md

# ...and the same file named the other way round. `.env.*` anchors on the
# leading dot, so a `<name>.env` file slips past it — `.prod.env` did, and was
# one `git add -A` away from being committed.
*.env
!.env.example

# ...and the same credentials under a name that is neither. `npx vercel env pull
# <file>` writes the whole production environment — DATABASE_URL, the Paystack
# secret, SESSION_SECRET, SHOP_AUTH_BRIDGE_SECRET — to whatever it is told, and
# the instruction to delete it afterwards is a step a person can forget. The
# rules above anchor on the word "env"; a scratch file called `.cron.tmp` slipped
# past every one of them and sat in the working tree unignored.
*.tmp

# The local demo database, images and mail (npm run demo). Rebuilt by the seed.
.demo-db/
